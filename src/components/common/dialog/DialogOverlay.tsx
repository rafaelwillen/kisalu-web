"use client";

import { DialogOverlay as Overlay } from "@radix-ui/react-dialog";

export default function DialogOverlay() {
  return <Overlay className="fixed inset-0 bg-neutral-900 bg-opacity-50" />;
}
